import React, { useState } from "react";

const BrowseFilterWrapper = (props) => {
    return (
        <div>
            {/* blah */}
            {/* stuff */}
        </div>
    );
};

export default BrowseFilterWrapper;
