import React from "react";

const BrowseResultsContainer = (props) => {
    return (
        <div>
            {/* stuff */}
            {/* blah */}
        </div>
    );
};

export default BrowseResultsContainer;
