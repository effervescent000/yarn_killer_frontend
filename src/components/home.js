import React from "react";

const HomePage = (props) => {
    return (
        <div className="content-wrapper">
            {/* stuff here */}
            {/* stuff here */}
        </div>
    );
};

export default HomePage;
