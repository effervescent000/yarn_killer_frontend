import React from "react";

const HomePage = (props) => {
    return (
        <div className="content-wrapper">
            Nothing here yet
            {/* stuff here */}
        </div>
    );
};

export default HomePage;
